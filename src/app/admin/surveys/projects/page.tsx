/**
 * @fileOverview SmartSapp Survey Intelligence 2.0 — Survey Projects Page
 */

import ProjectsClient from './ProjectsClient';

export const metadata = {
  title: 'Survey Research Projects | SmartSapp',
  description: 'Manage longitudinal survey programs, waves, and organizational research studies.',
};

export default function SurveyProjectsPage() {
  return <ProjectsClient />;
}
