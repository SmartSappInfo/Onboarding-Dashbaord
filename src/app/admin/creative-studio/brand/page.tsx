import * as React from 'react';
import { BrandStudioClient } from './BrandStudioClient';

export const metadata = {
  title: 'Brand Studio | Creative Studio 2.0',
};

export default function BrandStudioPage() {
  return <BrandStudioClient />;
}
