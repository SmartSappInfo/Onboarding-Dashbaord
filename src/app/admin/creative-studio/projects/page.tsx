import * as React from 'react';
import { ProjectsClient } from './ProjectsClient';

export const metadata = {
  title: 'Projects | Creative Studio 2.0',
};

export default function CreativeProjectsPage() {
  return <ProjectsClient />;
}
