import ProviderSettingsEditor from './components/ProviderSettingsEditor';

/**
 * Backoffice Provider Settings Page
 * Platform-wide view of integration keys and rate limit rules.
 */
export default function ProviderSettingsPage() {
  return <ProviderSettingsEditor />;
}
