import NewTemplateClient from './NewTemplateClient';

export const metadata = { title: 'New Message Template — Backoffice' };

export default function NewTemplatePage() {
  return <NewTemplateClient />;
}
