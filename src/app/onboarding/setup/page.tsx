import OnboardingSetupClient from './OnboardingSetupClient';

export const metadata = {
  title: 'Organization Setup - SmartSapp',
  description: 'Configure your organization branding and set up your first workspace.',
};

export default function OnboardingSetupPage() {
  return <OnboardingSetupClient />;
}
