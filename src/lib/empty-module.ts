/**
 * Empty module placeholder used by Turbopack/Webpack to stub server-only or native packages
 * that should not be bundled in client or build worker passes.
 */
const emptyModule: Record<string, unknown> = {};

export default emptyModule;
